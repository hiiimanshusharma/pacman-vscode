name: Publish to Open VSX
on:
  release:
    types: [published]
  workflow_dispatch:

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Compile Extension
        run: npm run compile
        
      - name: Publish to Open VSX
        run: npx vsce publish --registry https://open-vsx.org -p ${{ secrets.OVSX_PAT }}
